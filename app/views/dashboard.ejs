<% layout('layouts/authenticatedLayout') %>

<h1 class="mt-4">Dashboard</h1>
<a href="/foods/newFood" class="btn btn-primary">Add a new food</a>
<a href="/foods/allFoods" class="btn btn-primary">View All Foods</a>

<% if(userId) { %>
    <li>Yes! It works</li>
    <% } else {%>
<li>Didn't work, unfortunately</li>
<% } %>
<!-- check if user exists, if so do something-->
<p class="lead mb-3">Welcome <%= name %></p>